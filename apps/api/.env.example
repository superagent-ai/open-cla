NODE_ENV=development
PORT=3000
PUBLIC_APP_URL=http://localhost:3000
ADMIN_WEB_URL=http://localhost:3001

# Any Postgres-compatible provider works. Neon is the recommended initial DB.
DATABASE_URL=postgres://user:password@host:5432/superagent_cla?sslmode=require
DATABASE_MIGRATION_URL=postgres://user:password@host:5432/superagent_cla?sslmode=require

GITHUB_APP_ID=123456
GITHUB_APP_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\n...\n-----END RSA PRIVATE KEY-----"
GITHUB_WEBHOOK_SECRET=replace-with-webhook-secret
GITHUB_CLIENT_ID=Iv1.example
GITHUB_CLIENT_SECRET=replace-with-client-secret

# Optional but recommended for production personal signing.
# Create a separate GitHub OAuth App for admin sign-in (not the GitHub App itself).
GITHUB_OAUTH_CLIENT_ID=
GITHUB_OAUTH_CLIENT_SECRET=
# Comma-separated scopes. read:org lets org owners list private org repos in the admin UI.
# Set to empty to request no scopes (public repos / user-owned installs only).
GITHUB_OAUTH_SCOPES=read:org

# Minimum 32 characters.
SESSION_SECRET=replace-with-at-least-32-characters
# Optional: set for production when API and web are sibling subdomains.
# Example: COOKIE_DOMAIN=.superagent.sh
COOKIE_DOMAIN=
DEFAULT_CLA_TEMPLATE_NAME=standard-combined-v1
